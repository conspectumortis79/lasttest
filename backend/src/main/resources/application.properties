spring.application.name=lasttest
server.address=0.0.0.0
server.port=8286
spring.web.resources.static-locations=classpath:/static/,file:${LASTTEST_FRONTEND_PATH:../frontend/dist}/
spring.mvc.static-path-pattern=/**

# Jackson erlaubt case-insensitive Enum-Deserialisierung, damit das
# Frontend `ramping-vus` (kebab-case, executor-Name aus k6) statt
# `RAMPING_VUS` (Enum-Konstante) senden kann. Konsistent mit dem
# Rest des Wire-Formats, das kebab-case verwendet.
spring.jackson.mapper.accept-case-insensitive-enums=true

# InfluxDB-1.x-Defaults passen zum docker-compose-Setup und können
# via Umgebungsvariablen LASTTEST_INFLUXDB_* überschrieben werden.
# `enabled=false` schaltet die Time-Series-Integration komplett ab,
# falls kein InfluxDB gewünscht ist.
lasttest.influxdb.url=${LASTTEST_INFLUXDB_URL:http://influxdb:8086}
lasttest.influxdb.bucket=${LASTTEST_INFLUXDB_BUCKET:k6}
lasttest.influxdb.user=${LASTTEST_INFLUXDB_USER:k6-writer}
lasttest.influxdb.token=${LASTTEST_INFLUXDB_PASSWORD:lasttest-writer-password}
lasttest.influxdb.enabled=${LASTTEST_INFLUXDB_ENABLED:true}
